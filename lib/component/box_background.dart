import 'package:flutter/material.dart';

class BoxBackground extends StatelessWidget {
  const BoxBackground({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
